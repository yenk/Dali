# Core deps for corpus/ and runners/
# Required for Tier 1 local reference evaluator (no API keys needed):
# (none beyond stdlib — corpus/ uses only json, hashlib, dataclasses, re)

# Required for Tier 1 with --check-reachability:
# (uses urllib from stdlib — no extra deps)

# Required for Tier 2 synthetic probe runner (run_synthetic.py):
anthropic>=0.40.0
openai>=1.50.0
httpx>=0.27.0
trafilatura>=1.12.0
google-cloud-storage>=2.18.0
